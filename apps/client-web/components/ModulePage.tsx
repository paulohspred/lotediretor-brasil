import {Card,Badge} from '@lotediretor/ui';import {Shell} from './Shell';
export function ModulePage({title,description,steps,status='Em desenvolvimento'}:{title:string;description:string;steps:string[];status?:string}){return <Shell><main className="page"><header className="pageHead"><div><h1>{title}</h1><p>{description}</p></div><Badge tone="warning">{status}</Badge></header><div className="modulePlaceholder"><Card className="sidePanel"><b>Fluxo do módulo</b>{steps.map((s,i)=><div className="step" key={s}>{i+1}. {s}</div>)}</Card><Card className="workspace"><div><strong>Fundação conectada</strong><p>Esta rota já usa o shell, permissões e contratos definitivos. O motor específico entra nas próximas releases sem alterar navegação, banco-base ou fronteiras arquiteturais.</p></div></Card></div></main></Shell>}

export default async function Login({searchParams}:{searchParams:Promise<{returnTo?:string}>}){const p=await searchParams;const returnTo=(p?.returnTo?.startsWith('/admin/')||p?.returnTo?.startsWith('/app/'))?p.returnTo:'/app/dashboard';return <main className="loginPage"><section className="loginCard"><div className="brand" style={{color:'var(--ld-black)'}}>Lote<span>Diretor</span></div><h1>Acesse sua conta</h1><p>Use sua identidade LoteDiretor. A autenticação real acontece no provedor OIDC com PKCE e sessão HttpOnly.</p><a className="loginBtn" href={`/api/v1/auth/start?returnTo=${encodeURIComponent(returnTo)}`}>Entrar com conta LoteDiretor</a><div className="loginMeta"><span>Recuperar acesso</span><span>Ambiente seguro</span></div></section></main>}

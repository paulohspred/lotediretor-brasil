apiVersion: 1
providers:
  - name: LoteDiretor
    orgId: 1
    folder: LoteDiretor
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

import {Pool} from 'pg';import {readdirSync,readFileSync} from 'fs';import {join} from 'path';
export async function runMigrations(url:string, dir:string){const pool=new Pool({connectionString:url});await pool.query('CREATE TABLE IF NOT EXISTS public.schema_migrations(name text primary key, applied_at timestamptz not null default now())');for(const name of readdirSync(dir).filter(x=>x.endsWith('.sql')).sort()){const hit=await pool.query('select 1 from public.schema_migrations where name=$1',[name]);if(hit.rowCount)continue;const sql=readFileSync(join(dir,name),'utf8');const c=await pool.connect();try{await c.query('begin');await c.query(sql);await c.query('insert into public.schema_migrations(name) values($1)',[name]);await c.query('commit');console.log('migration',name)}catch(e){await c.query('rollback');throw e}finally{c.release()}}await pool.end()}
